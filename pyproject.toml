[build-system]
requires = ["setuptools>=42", "pybind11>=2.10"]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
archs = "all"
build-verbosity = 1

[tool.ruff]
target-version = "py38"
line-length = 88
lint.select = ["F"]

[tool.mypy]
ignore_missing_imports = true
