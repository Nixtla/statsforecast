



# <kbd>module</kbd> `statsforecast.theta`





---



## <kbd>function</kbd> `initparamtheta`

```python
initparamtheta(
    initial_smoothed: float,
    alpha: float,
    theta: float,
    y: ndarray,
    modeltype: ModelType
)
```






---



## <kbd>function</kbd> `switch_theta`

```python
switch_theta(model: str) → ModelType
```






---



## <kbd>function</kbd> `optimize_theta_target_fn`

```python
optimize_theta_target_fn(
    init_par: dict[str, float],
    optimize_params: dict[str, bool],
    y: ndarray,
    modeltype: ModelType,
    nmse: int
)
```






---



## <kbd>function</kbd> `thetamodel`

```python
thetamodel(
    y: ndarray,
    m: int,
    modeltype: str,
    initial_smoothed: float,
    alpha: float,
    theta: float,
    nmse: int
)
```






---



## <kbd>function</kbd> `compute_pi_samples`

```python
compute_pi_samples(
    n,
    h,
    states,
    sigma,
    alpha,
    theta,
    mean_y,
    seed=0,
    n_samples=200
)
```






---



## <kbd>function</kbd> `forecast_theta`

```python
forecast_theta(obj, h, level=None)
```






---



## <kbd>function</kbd> `auto_theta`

```python
auto_theta(
    y,
    m,
    model=None,
    initial_smoothed=None,
    alpha=None,
    theta=None,
    nmse=3,
    decomposition_type='multiplicative'
)
```






---



## <kbd>function</kbd> `forward_theta`

```python
forward_theta(fitted_model, y)
```






