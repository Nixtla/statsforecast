# AUTOGENERATED! DO NOT EDIT! File to edit: nbs/distributed.utils.ipynb (unless otherwise specified).

__all__ = ['forecast', 'cross_validation']

# Cell
from typing import Optional

from .core import ParallelBackend

# Cell
def forecast(
    df,
    models,
    freq,
    h,
    X=None,
    level=None,
    parallel: Optional["ParallelBackend"] = None,
):
    backend = parallel if parallel is not None else ParallelBackend()
    return backend.forecast(df, models, freq, h=h, X=X, level=level)

# Cell
def cross_validation(
    df,
    models,
    freq,
    h,
    n_windows=1,
    step_size=1,
    test_size=None,
    input_size=None,
    parallel: Optional["ParallelBackend"] = None,
):
    backend = parallel if parallel is not None else ParallelBackend()
    return backend.cross_validation(
        df,
        models,
        freq,
        h=h,
        n_windows=n_windows,
        step_size=step_size,
        test_size=test_size,
        input_size=input_size,
    )