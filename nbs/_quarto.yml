# Use default project type; mintlify-md format comes from _extensions/mintlify.
# (type: mintlify is only recognized by Quarto 1.4.x; 1.8+ fails with "Unsupported project type mintlify".)
project:
  type: default

format:
  mintlify-md:
    code-fold: true

